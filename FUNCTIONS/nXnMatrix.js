function solve(num) { 
    
    let rows = [] ; 
    
    for(let i = 0; i < num; i++) {

        for(let j = 0; j < num; j++) { 
            rows.push(num)
        }
        console.log(rows.join(' '));
        rows = []
     }

     
}
